# GigBlade

Plataforma multitenant para sitios de DJs. Un solo despliegue de Next.js resuelve tenants por hostname.

Consulta `ARCHITECTURE.md` antes de cambiar código. En esta fase no hay UI.

```bash
cp .env.example .env.local
npm install
npm run dev
```
